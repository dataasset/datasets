echo "# datasets" >> README.md

git add README.md

git add -A (to include all files and folders)

git commit -m "first commit"

git branch -M main

git remote add origin https://github.com/dataasset/datasets.git

git push -u origin main

